# 子阶段 2-A 验收报告（仅后端 + 数据结构）

> 分支 `arena/01a09b16-work123`｜基线 HEAD `3c4f27b`（「项目层级」现状排查）
> 验收时间：2026-09-17｜验收方式：**真机**（真 Fastify 后端 + 真 PostgreSQL + 真 HTTP + 真 JWT）
> 证据目录：`docs/acceptance/substage-2a/`｜复跑脚本：`scripts/verify/2a-*.mjs`
> 本轮结论：**四条硬指标全部通过（79 条断言 0 失败）**，过程中抓出并修掉 2 个真 bug。

---

## 0. 一句话结论

「项目」这一层从「每个用户一条默认项目 + 只当鉴权中转」升级成了**真正的容器**：
有 `/projects` 接口、有「当前使用中的项目」、有随项目创建的**母鸡**（`can_create_agents` 权限闸）、
知识库也补齐了**项目归属并完成无损迁移**。前端、浏览器分区、母鸡的「调度」能力**一行未动**。

---

## 1. 交付范围与边界

### 1.1 做了（对齐子阶段 2-A 的 5 条要求）

| # | 要求 | 落点 | 状态 |
|---|---|---|---|
| 1 | 新增 `/projects`：创建 / 列出 / 重命名 / 设为当前 | `apps/server/src/routes/projects.ts`（新建）+ `projectScope.ts`（新建） | ✅ |
| 2 | `GET /agents` 增加按 `project_id` 过滤 | `routes/agents.ts` `GET /agents` | ✅ |
| 3 | 母鸡机制：随项目创建、不可删、权限开关、建智能体校验 | `projectScope.ts` + `routes/agents.ts` + `db.ts` | ✅ |
| 4 | 知识库项目归属 + 数据迁移 | `db.ts` `migrateProjectScope()` + `routes/knowledge.ts` | ✅ |
| 5 | `agent_memories` 不动 | 未触碰（DDL / 读写路径均无改动） | ✅ |

### 1.2 明确没做（本阶段边界，逐条核对过）

- **前端一行未改**：`apps/desktop/**` 与 `packages/shared` 的 UI 类型无改动（shared 只加了「项目/权限」字段）。
- **浏览器分区未改造**：`partitionFor()` 仍是 `persist:workbench-browser-agent-{agentId}`。
  （因此**现有登录态不会作废**，这是本阶段刻意保留的。）
- **母鸡没有「调度其他智能体」的能力**：只做了「创建智能体」这一项最小权限。
- 没做：删项目、项目级配额、项目级记忆、按项目分浏览器。

---

## 2. 数据结构变更

```sql
-- 当前使用中的项目；为空时回落到 is_default 那条（老账号行为一行不变）
ALTER TABLE users              ADD COLUMN IF NOT EXISTS current_project_id BIGINT REFERENCES projects(id) ON DELETE SET NULL;
-- 权限开关：能不能建智能体。母鸡/自带小助 true；用户自建智能体默认 false
ALTER TABLE agents             ADD COLUMN IF NOT EXISTS can_create_agents BOOLEAN NOT NULL DEFAULT false;
-- 知识库项目归属（老数据由 migrateProjectScope 按 owner 各自回填到默认项目）
ALTER TABLE knowledge_documents ADD COLUMN IF NOT EXISTS project_id BIGINT REFERENCES projects(id) ON DELETE CASCADE;
ALTER TABLE knowledge_chunks    ADD COLUMN IF NOT EXISTS project_id BIGINT REFERENCES projects(id) ON DELETE CASCADE;
-- 两个索引 + 回填确认无空值后把 project_id 收紧为 NOT NULL
```

`projects` / `agents.project_id` / `conversations.project_id` / `tasks.project_id` / `memories.project_id`
**本来就存在**（第 5 步建的），本次没有重建、没有改外键。

**两处不变量**（`db.ts` 的 `migrateProjectScope()`，每次启动幂等执行，只补空值）：

1. `users.current_project_id` 为空 → 取该用户 `is_default` 那条（老账号 = 什么都没发生）。
2. `kind IN ('assistant','hen')` 的行权限必须为 true → 老库补上，老账号照样能点「＋ 添加」。
3. 知识库 `project_id` 为空 → 按 **owner** 各自挂到自己的默认项目（不是一刀切挂到某一个项目）。

---

## 3. 接口清单

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/projects` | 我的项目列表，标出 `isCurrent`；每条带 `henAgentId` |
| POST | `/projects` `{name}` | 建项目 → **同一事务里连带建一只母鸡**（`kind='hen'`、`can_create_agents=true`、`persona_status='ready'`、含一条空会话）→ 设为当前项目 |
| PATCH | `/projects/:id` `{name}` | 重命名（≤24 字，空白折叠） |
| POST | `/projects/:id/activate` | 设为「当前使用中的项目」 |
| GET | `/agents?projectId=` | 可选按项目过滤；不传 = **改造前的行为**（账号全部智能体） |
| POST | `/agents` `{asAgentId?}` | 建智能体前**校验调用者权限**；新人落在「当前项目」 |
| GET | `/knowledge?projectId=` | 项目隔离；不传 = 当前项目 |

三条统一口径：归属一律来自 JWT 的 `claims.sub`（不接受客户端传 owner）；
不是自己的项目/资料一律 **404**（不区分「不存在」与「是别人的」，避免用 id 探测）；
内置角色（小助 / 母鸡）**不可删**、**有建智能体权限**。

---

## 4. 四条硬指标的真机取证

### ① 母鸡不可删除 —— 通过

后端权威闸 + 前端两条入口都盖住了，并且做了**正对照**（不是「一律拒删」）：

```
PASS  **母鸡不可删除**：DELETE 被后端拒绝（400）
      :: status=400 body={"error":"这是项目的母鸡（随项目创建、有建智能体的权限），不能删。"}
PASS  母鸡被拒删后**数据库里那行仍在**（不只是回了个错误码） :: agents(id=49) 命中 1 行
PASS  **前端也拦得住**：接口给母鸡的 deletable=false（App.tsx 只按这个字段决定画不画「删掉」按钮）
PASS  前端那条**唯一没按 deletable 判**的删除入口也进不去：母鸡 personaStatus 恒为 ready → 引导表（内含删除按钮）永不渲染
PASS  自带小助仍然不可删（老行为回归） :: status=400
PASS  正对照：普通智能体删得掉（DELETE 200 + 库里那行没了）—— 闸门只挡内置角色 :: status=200 剩余行=0
PASS  母鸡在整轮测试跑完后依然在（从头到尾没被删掉过）
```

**前端为什么不用改也拦住了**（两条路，都核对过 `App.tsx`）：

- `App.tsx:1641` 的「删掉「X」」按钮：条件是 `curAgent.deletable` → 母鸡 `deletable=false`，按钮根本不渲染（grep 确认这是唯一的列表删除入口）。
- `App.tsx:1900` 的 `AgentGuide onDelete`：**这个没有按 `deletable` 判**，但它只在 `personaStatus === 'pending'` 时渲染。
  后端 `toAgentView()` 对内置角色一律返回 `'ready'` → **这条入口在母鸡身上永远渲染不出来**。
  （见 §8 的 bug #2，就是这轮为它补的。）

### ② 权限校验生效 —— 通过

```
PASS  普通智能体默认没有建智能体的权限 :: canCreateAgents=false
PASS  **权限校验生效**：普通智能体作为调用者 → 403
      :: body={"error":"「新智能体」没有创建智能体的权限 —— 只有项目里的母鸡和自带的「小助」可以建智能体。"}
PASS  403 时**没有产生半成品数据** :: agents in project 18: 2 -> 2
PASS  把同一个智能体的开关改成 true → 立刻能建（闸门认字段，不是认 kind） :: status=200
PASS  开关改回 false → 又 403（可逆）
PASS  母鸡作为调用者可以建智能体 :: status=200
PASS  调用者不存在 → 404 ｜ 拿**别人的**智能体当调用者 → 404（归属校验）
```

关键设计（**这条要总控确认，见 §9**）：`POST /agents` 的调用者 = `body.asAgentId`；
**不带**时回落成「这个账号自带的小助」。这样**改造前的前端**（点「＋ 添加」不带任何 body）行为一行不变，
而校验本身**一次都不会被跳过**（校验的永远是某个真实智能体的权限位）。

### ③ 知识库数据迁移无损 —— 通过（对照表见 §5）

### ④ `GET /agents` 按项目过滤正确 —— 通过

```
PASS  带 projectId 只回该项目里的智能体（默认项目） :: default项目=[48,51,52] 母鸡id=49
PASS  带 projectId 只回该项目里的智能体（新建项目） :: 新项目=[49]，默认项目=[48,51,52]（两边无交集）
PASS  两个项目的结果集互补且都在「不带参数」的全集里（过滤没漏也没多） :: all=[48,49,51,52]
PASS  不带参数 = 改造前行为（该账号全部智能体），前端不会被悄悄改掉
PASS  排序：小助 → 母鸡 → 普通（同一账号内小助仍排最前） :: 48:assistant 49:hen 51:custom 52:custom
PASS  查**别人的**项目 → 404 ｜ projectId 非数字 → 400 ｜ projectId=0 → 400
```

---

## 5. 知识库迁移：迁移前后条数对照（重点证据）

**为什么要在独立库里跑**：活库在开发过程中**已经跑过一次**本迁移（列早就存在、也已经有归属了），
直接对比等于「迁移后 vs 迁移后」。所以脚本 `2a-migrate-replay.mjs` 做了四件事：

1. 新建**只在验收期存在**的库 `workbench_2a_check`；
2. 用 **`git show HEAD:apps/server/src/db.ts` 里的真实 DDL** 建出「迁移前」结构
   （脚本先断言 4 个新列**都不存在**，证明这确实是真的迁移前）；
3. 把**活库真实数据**按旧结构整表搬进去（列清单天然不含新列）；活库只 `SELECT`；
4. 调**构建产物 `dist/db.js` 里真正的 `migrate()`**，前后各拍一次快照。

### 5.1 行数对照（迁移前 → 迁移后）

| 表 | 迁移前 | 迁移后 | 差 |
|---|---|---|---|
| users | 3 | 3 | 0 |
| projects | 4 | 4 | 0 |
| agents | 7 | 7 | 0 |
| conversations | 7 | 7 | 0 |
| messages | 519 | 519 | 0 |
| tasks | 212 | 212 | 0 |
| memories | 39 | 39 | 0 |
| user_memories | 3 | 3 | 0 |
| agent_memories | 1 | 1 | 0 |
| **knowledge_documents** | **92** | **92** | **0** |
| **knowledge_chunks** | **722** | **722** | **0** |

> 知识库两表的 92 / 722 = **活库真实 2 / 2** + **合成放大样本 90 / 720**。
> 为什么加样本：活库里带资料的只有 **1 个 owner**（另两个一条都没有），光靠真实数据**证明不了多账号不串**，
> 所以在迁移前的结构里给 3 个 owner 各塞了 30 份资料 × 8 个片段，和真实数据一起走迁移。
> 合成样本**只存在于验收库**，跑完连库一起删。

### 5.2 逐 owner 对照（多账号不串）

| owner | 资料 迁移前 | 资料 迁移后 | 片段 迁移前 | 片段 迁移后 |
|---|---|---|---|---|
| 1 | 32 | 32 | 242 | 242 |
| 2 | 30 | 30 | 240 | 240 |
| 3 | 30 | 30 | 240 | 240 |

### 5.3 不只对行数，还对**内容**

| 验收项 | 结果 |
|---|---|
| 知识库内容摘要（`filename_enc` / `byte_size` / `chunk_count` / `content_enc` / 段序 全部按 id 拼起来做 sha256） | 前 `3d2ad04e…` → 后 `3d2ad04e…`（资料）／`6f5526a6…` → `6f5526a6…`（片段）**完全一致** |
| 每条资料/片段都拿到归属（逐条核对 = 该 owner 的默认项目；片段还必须与它所属资料一致） | 资料 92 条 / 片段 722 条，**不符 0 条** |
| 迁移后 `project_id` 空值 | documents=0，chunks=0 |
| `project_id` 是否已收紧 | `is_nullable = NO`（两张表都是） |
| `users.current_project_id` 空值 | 0 行（老账号 = 自己的默认项目） |
| **幂等**：再跑一遍 | 上报「补 0 行 / 回填 0 条」，行数与摘要不变 |
| **不覆盖用户已选过的当前项目** | 手工把某用户设为非默认项目 10 → 再迁移一遍 → 仍是 10 |

### 5.4 迁移在真实活库上的可见结果（只读回归顺带验证）

老账号（user 1）在活库里有 2 份历史资料，迁移后各归各家，通过接口就能看出来：

```
当前项目=1  资料=[] 7@p1 9@p10   接口回=[7]          ← 不带参数只看当前项目
GET /knowledge?projectId=1  → ids=[7]   ✅（迁移回填到默认项目的那份）
GET /knowledge?projectId=10 → ids=[9]   ✅（后来在新项目里上传的那份）
```

---

## 6. 全套复跑方式

```bash
node scripts/verify/2a-migrate-replay.mjs                  # 迁移前后对照（自建/自删 workbench_2a_check）
node scripts/verify/2a-api-tests.mjs                       # 接口真机验收（自建 8799 后端 + 临时账号，跑完自删）
node scripts/verify/2a-regression-existing-account.mjs     # 老账号只读回归（8798，11 张表零改动）
node scripts/verify/db-snapshot.mjs <out.json>             # 只读数据快照（任意时刻采基线）
```

三个脚本的共同纪律：**用户自己的 8787 / 5173 一律不碰**；`2a-api-tests` 全程只用**本次新建的测试账号**
（手机号自动挑一个库里没有的），跑完整体级联删除并在报告里断言「活库行数与逐表 id 集合回到初始状态」。

---

## 7. 代码改动清单

**上一轮已完成、本轮验收通过的部分**（此前未提交）：

| 文件 | 改动 |
|---|---|
| `apps/server/src/projectScope.ts` | **新增**：当前项目 / 项目列表 / 建项目带母鸡 / 调用者权限解析（服务端唯一出口） |
| `apps/server/src/routes/projects.ts` | **新增**：`GET/POST /projects`、`PATCH /projects/:id`、`POST /projects/:id/activate` |
| `apps/server/src/db.ts` | DDL 补 4 列 + 2 索引；新增幂等迁移 `migrateProjectScope()` |
| `apps/server/src/routes/agents.ts` | `GET /agents` 支持 `projectId`；`POST /agents` 校验权限 + 落当前项目；`DELETE` 增加母鸡硬闸；`toAgentView` 补 `projectId/deletable/canCreateAgents` |
| `apps/server/src/routes/knowledge.ts` | 列表/上传按项目隔离；`buildKnowledgeBlock()` 增加 `projectId` 参数 |
| `apps/server/src/routes/chat.ts` | 知识库检索按**这条会话所属项目**；无 agentId 的兜底会话改按当前项目找 |
| `apps/server/src/routes/auth.ts` | 登录/建号回**当前项目**（回落默认项目） |
| `apps/server/src/routes/agent.ts`、`memories.ts` | 任务与 `memories` 落点改「当前项目」 |
| `packages/shared/src/index.ts` | 增量：`ProjectSummary` 扩字段 + `ProjectListResult/ProjectCreateResult/ProjectUpdateResult`；`AgentView` 加 `projectId/canCreateAgents`；`KnowledgeDocument` 加 `projectId` |
| `apps/server/src/index.ts` | 注册项目路由 |
| `apps/desktop/**` | **零改动** |

**本轮新增（验收资产）**：`scripts/verify/2a-migrate-replay.mjs`、`2a-api-tests.mjs`、
`2a-regression-existing-account.mjs`、`db-snapshot.mjs`，
`docs/acceptance/substage-2a/`（本报告 + 3 份 JSON 证据 + 服务端日志）。

---

## 8. 本轮抓出并修掉的 2 个真 bug（别再踩）

### bug #1 —— **服务运行期间新注册的账号，小助没有建智能体的权限**

`auth.ts` 建号时插小助那句 `INSERT INTO agents (project_id, name, kind)` **没带 `can_create_agents`**，
列的默认值是 `false` → 新账号一点「＋ 添加」直接 403。
原来那套「启动时不变量回填」只在**重启时**跑，**盖不住运行期间新建的行**。

- 触发方式：起后端 → 用新手机号注册 → `POST /agents` → 403（就是本轮接口验收第一条 FAIL）。
- 修法：建号那句**显式写 `can_create_agents = true`**（母鸡的建法本来就是显式的，两边对齐）。
- 教训：**列的默认值是给「用户自建智能体」的**；内置角色必须在每一个插入点显式给值。

### bug #2 —— **前端有一条删除入口绕过了 `deletable`**

`App.tsx:1900` 的 `<AgentGuide onDelete>` **没有**按 `deletable` 判，只在 `personaStatus === 'pending'` 时渲染。
本阶段不允许改前端，所以从后端把这条路的触发条件消掉：`toAgentView()` 对内置角色（小助 / 母鸡）
**一律返回 `personaStatus: 'ready'`** → 引导表永不为它们渲染 → 那条无守卫的删除入口**结构性不可达**，
后端 `DELETE` 仍是权威闸（400）。

---

## 9. 已知限制 / 需要总控确认的取舍点

1. **`POST /agents` 不带 `asAgentId` 时回落到「自带小助」**（而不是硬性 403）。
   好处：改造前的旧前端行为一行不变、校验不被跳过；代价：权限闸的「调用者」是推断出来的。
   → 前端接上项目层后，建议改成**显式传当前智能体**；在那之前这是最不破坏现状的选法。
2. **新智能体落在「当前使用中的项目」，不是「调用者所在项目」**。
   跨项目调用（在项目 A 里用项目 B 的母鸡建智能体）时会落到 A。当前实现下这是唯一自洽的口径
   （「我在哪个项目，新建的就在哪个项目」），但 2-B / 前端接上后需要重新确认一次。
3. **知识库检索按「会话所属项目」过滤**，不再按账号。
   直接后果：一份挂在项目 B 的资料，在项目 A 的会话里**检索不到**（这正是「按项目隔离」的语义）。
   老会话都在默认项目里 → 对老用户无感。
4. **项目不可删除**（本阶段边界），`is_default` 那条永远保留兜底。
5. `/projects` 列表 `LIMIT 50`；项目名 ≤ 24 字。
6. `session.project` / `AgentView.projectId` 已经下发，但**前端一次都没读**（2-A 不动前端）。
7. `can_create_agents` 是**读时权威**：接口只认这个字段（内置角色也靠它），
   没有另加「kind 是 hen 就一定有权限」的隐式兜底 —— 这样开关是可控的；
   代价是任何新建内置角色的代码都必须显式写 true（已用 bug #1 的注释钉住）。

---

## 10. 收尾与环境状态

- **活库零污染**：接口验收跑完，11 张表行数与**逐表 id 集合**都与跑之前完全一致
  （`users=3 / projects=4 / agents=7 / conversations=7 / messages=519 / tasks=212 / memories=39 /
  user_memories=3 / agent_memories=1 / knowledge_documents=2 / knowledge_chunks=2`）；
  测试账号（含它的项目/母鸡/临时资料）已整体级联删除。
- **验收库已删**：`workbench_2a_check` 跑完即 `DROP`（活库全程只读）。
- **端口**：验收用的 8799 / 8798 实例已随脚本收尾关闭；用户自己的 8787 / 5173 **未动**。
- `npm run typecheck`（shared + desktop + server）与 `npm run build -w @ai-workbench/server` 均通过。
- 唯一没有覆盖到的区域：桌面端 Electron 侧的 UI 表现（本阶段不允许动前端，也不在范围内）。

---

## 11. 提交与收尾状态

- **本地已提交**：`f2aac37`（父 `3c4f27b`，分支 `arena/01a09b16-work123`，24 个文件）。
  `git diff HEAD` 为空、工作区干净、`merge-base --is-ancestor 3c4f27b HEAD` 成立。
- **未 push、未合并 main**（等总控指令）。
- 子阶段 2-A 到此为止，等总控批：是否进入 2-B（前端接项目层 / 浏览器分区按项目 / 母鸡调度），
  以及 §9 里第 1、2 条取舍要不要现在定下来。
